<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <% pageTitle = strings.storyEditor.title %>
    <% extraCss = '/css/story-editor.css' %>
    <%- include('../partials/head') %>
</head>
<body>
    <%- include('../partials/nav') %>

    <div class="container full-height">
        <div class="admin-header">
            <h1><%= strings.storyEditor.title %></h1>
            <div id="status-message" class="status-message"></div>
        </div>

        <div class="editor-layout">
            <!-- Sidebar: File and Scene List -->
            <div class="editor-sidebar">
                <div class="sidebar-section">
                    <h3><%= strings.storyEditor.files %></h3>
                    <select id="file-select" class="form-control">
                        <option value=""><%= strings.storyEditor.selectFile %></option>
                    </select>
                </div>
                <div class="sidebar-section scrollable" id="scene-list-container">
                    <h3><%= strings.storyEditor.scenes %></h3>
                    <ul id="scene-list" class="scene-list">
                        <!-- Scenes populated by JS -->
                    </ul>
                </div>
            </div>

            <!-- Main: Scene Content Editor -->
            <div class="editor-main" id="editor-pane" style="display: none;">
                <div class="scene-header">
                    <h2 id="current-scene-id"><%= strings.storyEditor.selectScene %></h2>
                    <div class="scene-meta">
                        <label><%= strings.storyEditor.location %>:</label>
                        <input type="text" id="scene-location" class="form-control-inline">
                    </div>
                    <button id="save-btn" class="btn btn-primary"><%= strings.storyEditor.save %></button>
                </div>

                <div class="editor-sections">
                    <div class="editor-section">
                        <h3><%= strings.storyEditor.script %></h3>
                        <textarea id="scene-script" class="form-control editor-textarea"></textarea>
                    </div>
                    <div class="editor-section">
                        <h3><%= strings.storyEditor.actions %></h3>
                        <textarea id="scene-actions" class="form-control editor-textarea"></textarea>
                    </div>
                </div>
            </div>

            <div class="editor-placeholder" id="editor-placeholder">
                <p><%= strings.storyEditor.placeholder %></p>
            </div>
        </div>
    </div>

    <script src="/js/story-editor.js"></script>
</body>
</html>
