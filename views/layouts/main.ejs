<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= typeof title !== 'undefined' ? title + ' - BichonWebsite' : 'BichonWebsite' %></title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav>
        <div class="nav-content">
            <a href="/" class="logo">BichonWebsite</a>
            <div class="nav-links">
                <a href="/">Home</a>
                <% if (isAuthenticated) { %>
                    <a href="/admin">Dashboard</a>
                    <a href="/admin/posts">Manage Posts</a>
                    <a href="/admin/logout">Logout</a>
                <% } else { %>
                    <a href="/admin/login">Admin</a>
                <% } %>
            </div>
        </div>
    </nav>

    <div class="container">
        <% if (typeof message !== 'undefined' && message) { %>
            <div class="message message-<%= messageType || 'success' %>">
                <%= message %>
            </div>
        <% } %>

        <%- typeof body !== 'undefined' ? body : '' %>
    </div>

    <footer>
        <p>&copy; <%= new Date().getFullYear() %> BichonWebsite. All rights reserved.</p>
    </footer>
</body>
</html>
