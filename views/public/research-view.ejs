<!DOCTYPE html>
<html lang="<%= lang %>">
<head>
    <title><%= filename.replace(/\.md$/, '').replace(/_/g, ' ') %></title>
    <%- include('../partials/head') %>
    <link rel="stylesheet" href="/css/report.css?v=<%= Date.now() %>">
    <script src="https://cdn.jsdelivr.net/npm/marked/marked.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/dompurify/3.0.6/purify.min.js"></script>
</head>
<body>
    <%- include('../partials/nav') %>

    <div class="container">
        <article class="post-content">
            <h1><%= filename.replace(/\.md$/, '').replace(/_/g, ' ') %></h1>
            <div class="report-body" id="report-content"></div>
            <script type="application/json" id="report-raw"><%- JSON.stringify(markdown).replace(/</g, '\\u003c') %></script>
        </article>

        <div class="box post-nav">
            <div class="post-nav-left">
                <a href="/reports">&larr; <%= strings.public.backToList %></a>
            </div>
        </div>
    </div>

    <%- include('../partials/footer') %>
    <script src="/js/report-view.js?v=<%= Date.now() %>"></script>
</body>
</html>
