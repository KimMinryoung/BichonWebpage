<div class="home-toolbar">
    <a href="/" class="toolbar-logo"><%= strings.siteName %></a>
    <div class="toolbar-center">
        <% if (currentUser) { %>
            <span class="toolbar-user">@<%= currentUser.username %></span>
            <form action="/auth/logout" method="POST" class="toolbar-inline-form">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit" class="toolbar-link"><%= strings.userAuth.logout %></button>
            </form>
        <% } else { %>
            <a href="/auth/login" class="toolbar-link"><%= strings.userAuth.login %></a>
        <% } %>
    </div>
    <div class="toolbar-right">
        <button type="button" class="theme-toggle" id="themeToggle" title="Toggle theme">&#x263E;</button>
        <div class="lang-dropdown">
            <button type="button" class="lang-btn" id="langBtn">&#x1F310;<%= lang === 'en' ? 'English' : 'Korean' %></button>
            <div class="lang-menu" id="langMenu">
                <a href="#" data-lang="ko" class="<%= lang === 'ko' ? 'active' : '' %>">Korean</a>
                <a href="#" data-lang="en" class="<%= lang === 'en' ? 'active' : '' %>">English</a>
            </div>
        </div>
    </div>
</div>
<nav>
    <div class="nav-content">
        <div class="nav-links">
            <a href="/chat"><%= strings.nav.chat %></a>
            <a href="/posts"><%= strings.nav.bichonPosts %></a>
            <a href="/reports"><span class="nav-label-full"><%= strings.nav.reports %></span><span class="nav-label-short"><%= strings.nav.reportsShort %></span></a>
            <a href="/hub"><%= strings.nav.hub %></a>
            <a href="/ai-diary"><span class="nav-label-full"><%= strings.nav.diary %></span><span class="nav-label-short"><%= strings.nav.diaryShort %></span></a>
        </div>
    </div>
</nav>
<% if (isAuthenticated) { %>
<nav class="admin-bar">
    <div class="nav-content">
        <div class="nav-links">
            <a href="/admin"><%= strings.nav.dashboard %></a>
            <a href="/admin/posts"><%= strings.nav.posts %></a>
            <a href="/admin/passkeys"><%= strings.admin.passkeys %></a>
            <a href="/game"><%= strings.nav.game %></a>
            <form action="/admin/logout" method="POST" style="display:inline;margin:0;">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit" class="nav-logout-btn"><%= strings.nav.logout %></button>
            </form>
        </div>
    </div>
</nav>
<% } %>
<script src="/js/nav.js?v=<%= Date.now() %>"></script>
