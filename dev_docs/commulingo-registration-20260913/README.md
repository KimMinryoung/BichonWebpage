# 입문 강좌 신규 데이터 등록 — 2026-09-13

운영 등록 완료: **41건(인물 6명, 용어 16개, 원사료 번역 19편)**. 오언의 『사회에 대한 새로운 견해』 번역 1건은 미완료이며 공개하지 않았다. 운영 큐 1943~1984의 결과는 `queue-verification.json`에 있다.

## 등록과 검증

- 인물은 `scripts/commulingo-people-upsert`의 dry-run과 실제 Admin upsert를 통과했다. `people.json`이 제출 원본이며 승인 제안 ID는 9677~9682다. 인물 테이블에 직접 INSERT하지 않았다.
- 용어는 `commulingo-term-service.js`의 read → dry-run submit → submit → review 승인 경로를 거쳤다. `terms.json`, `term-receipts.json`에 제출 내용과 승인 결과가 있다.
- 문헌 공개본은 `data/commulingo/docs/`의 HTML과 manifest에 있다. `doc-receipts.json`은 19편의 등록 기록이다. 원어 전사·선택 범위·해시는 `specs/`, 실제 저본은 `sources/`에 보존했다.
- 기존 번역 파이프라인으로 비공개 초안을 생성한 후 조항·절의 범위, 핵심 주장과 논적 인용, 날짜·숫자 경고, 주석을 점검했다. `reviewed/*.review.json`에 수정 기록을 남겼다. 장문 전체에 대한 전문 번역가의 교열 완료를 뜻하지 않는다.
- 공개본에는 수동 교정과 주석 서식이 있으므로 해당 19개 스펙은 `frozen`으로 잠갔다. 재실행으로 교정본을 덮어쓰지 않는다.
- `npm test` 전체 통과(Node 20 지정 Docker 환경). 19편 모두 운영 origin에서 HTTP 200, 모바일 가로 넘침·깨진 주석 링크 없음. 공개 도메인에서도 최고가격법과 룩셈부르크의 주석 왕복을 브라우저로 확인했다. `browser-verification.json` 참고.
- `french-revolution-intro`, `socialist-divergences-intro`의 기존 입문 해설은 manifest에서 제외된 상태로 유지했고 `/commulingo/docs/<id>`가 404임을 확인했다. 이번에 공개한 문서는 원사료 번역이다.

## 저본·범위에서 확인한 차이

- 1791년 헌법에 앞선 1789년 권리 선언은 별도 문헌이다. Élysée 페이지의 현대 링크 문구는 법문에서 제거했다.
- 구주의 선언은 국민의회가 제공하는 도입문·서문·17개 조항이며 후기·헌정은 없다. 제목에 발췌를 명시했다.
- 생필품 연설은 전사본 앞부분의 괄호 삽입 진위를 확인하지 못해, 그 문장을 포함하지 않는 중후반부부터 결론까지 연속 발췌했다. 전문으로 등록하지 않았다.
- 정치도덕 보고의 1866년 베르모렐 선집은 편집자가 직접 축약 사실을 밝혔다. 발췌로 표시하고, 편집주에 인용된 1793년 12월 25일의 다른 보고를 섞지 않았다.
- 프레리알법은 채택 재록본의 제1~20조이며 임명자 명단이 빠져 있다. 다른 재록본의 제21조도 이 판본에는 없다. 발췌 제목과 서지에 명시했고 잘못된 제13조 번호를 대조 수정했다.
- 최고가격법은 『의회기록』 321~323쪽 인쇄 이미지와 OCR을 대조했다. 제1~20조, 가격·임금 상한, 징발, 벌칙을 모두 보존했다.
- 평등파 선언의 실제 채택 재록본은 1873년판이다. 초기 큐의 탐색 후보 판본과 구별했다.
- 베른슈타인의 「민주주의와 사회주의」는 채택 독일어 원판의 제4장 c절이다.
- 룩셈부르크 『사회개혁이냐 혁명이냐』는 MIA가 1908년 제2판에 따른다고 명시한 1982년 저작집 전사다. 『러시아 혁명』은 1918년 미완성 원고, 1922년 사후 발표, 2000년 저작집 제6개정판의 네 절과 저자 난외 메모를 채택했다. 영어판의 장 구분과 다르다.
- 『러시아 혁명』 제3절 뒤의 저자 메모 두 개가 원문 페이지의 이동 링크 아래에 있어 추출에서 빠진 것을 발견했다. 두 메모를 별도로 원문 대조 번역하여 복원하고 제4절의 메모와 섞이지 않도록 연결했다.
- 레닌은 제5장 전체다. 책 전체에서 고른 장이라는 의미와 장 내부 생략을 혼동하지 않도록 제목을 “제5장 전문”으로 했다.

## 오언 번역 대기: 큐 1977

### 다음 작업자 인수인계 — 2026-09-14 재개 예정

사용자 요청(2026-09-13): “내일 할 수 있게 인수인계 문서에 메모해.” 다음 작업은 **오언 1편의 남은 번역과 검토·등록**이다. 이미 완료한 41건을 다시 등록하지 않는다. 이 메모는 예약 실행이 아니며 자동 재개 작업은 설치하지 않았다.

- 중단 원인: 기존 번역 경로의 **Gemini API 프로젝트·모델별 일일 요청 한도**. 오류의 모델은 `gemini-3.1-pro`, quota ID는 `GenerateRequestsPerDayPerProjectPerModel`, 당시 한도는 250회/일이었다. CommuLingo 자체 예산 제한과 구별한다. 오류 원문 로그는 호스트 `/tmp/course-translations-more.log`에 있다.
- 다음 날에도 재설정 완료를 단정하지 말고 공급자 응답을 확인한다. 모델 라우팅이나 한도를 임의로 변경하지 않는다.
- 아래 스펙과 캐시로 **오언만** 실행한다. `/tmp/run_more_course_translations.py`는 여러 문헌을 순회하는 임시 스크립트이므로 그대로 재실행하지 않는다. 공개된 19편의 스펙은 수동 교정본 보호를 위해 frozen 상태다.
- 캐시와 미완성 출력은 이 디렉터리의 `.gitignore`에 따라 Git에서 제외되어 **현재 호스트에만 보존**된다. 새 체크아웃만으로는 39개 완료 청크를 복원할 수 없으므로 기존 호스트의 `translations/`를 지우지 않는다. 모델·프롬프트 변경 시 캐시 재사용 여부도 먼저 확인한다.

완료 절차:

1. `owen-new-view-society` 스펙과 기존 캐시를 사용해 남은 28개 청크를 재개한다. 재실행 결과의 성공·실패 수를 다시 확인하고, `failures=[]` 및 실패 표시가 없는 완성본을 확보한다.
2. 영어 저본의 서론·네 논설 전체 범위, 번역 내용과 주석을 검토한다. 논설 초판 1813~1814년과 채택 합본 1816년을 서지에서 구별한다.
3. 검토본을 `data/commulingo/docs/`에 원자적으로 반영하고 manifest에 등록한다. 스펙을 frozen으로 잠그고 검토 기록과 `doc-receipts.json`을 갱신한다.
4. 실제 공개 결과를 확인한 뒤 큐 1977을 done으로 처리하고 `queue-verification.json`, 상위 `commulingo-course-registration-queue-20260913.json`, 이 문서의 집계를 42건 완료로 갱신한다. `/tmp/finalize_course_queue.py`는 오언 pending을 하드코딩하므로 재사용하지 않는다.
5. 운영 문서 페이지의 HTTP 응답, 모바일 줄바꿈·가로 넘침, 주석 링크를 브라우저로 검증하고 이번 변경만 커밋·푸시한다. 작업 트리의 다른 진행 중 변경은 보존한다.

일일 공급자 한도(`429 RESOURCE_EXHAUSTED`, 요청 당시 모델별 250회/일)에 도달했다. 67개 청크 중 39개 번역이 캐시에 있고 28개가 남았다. 실패 청크 표시가 들어 있는 `translations/owen-new-view-society.html`은 **공개 금지**이며 manifest에도 없다.

호스트에 남긴 재개 자료:

- `specs/owen-new-view-society.json` (비공개 output, frozen 없음)
- `translations/owen-new-view-society.cache.jsonl` 및 parts/review/result 파일
- 원어 저본: `sources/owenintro.html`, `owen1.html`~`owen4.html`

공급자 한도가 회복된 뒤 backend `runtime_tools.archival_translation.core.run(spec, Options(cache_path=..., concurrency=...), progress)`에 같은 스펙·캐시를 전달하면 성공한 청크를 재사용할 수 있다. `failures`가 빈 배열인지 확인하고 원문 범위·내용·주석을 검토한 뒤 공개해야 한다. 기본 doc gap worker는 자동 번역·발행하지 않으므로 자동 재개를 약속하거나 별도 타이머를 설치하지 않았다.

기존 자동 editorial pipeline 작업 30834~30855는 실제 Admin 등록 결과에 맞춰 완료로 정리했다. 등록된 입헌군주제와 중복된 초안 9683은 검토 서비스로 거절 처리했다. 공급자·서비스의 예산이나 일일 한도는 변경하지 않았다.
