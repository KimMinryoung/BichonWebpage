# CommuLingo 인물 등록·수정·보강 개선

작성일: 2026-09-07. 사용자 승인에 따라 순서대로 구현한다. 체크는 구현과 검증이 끝난 항목에만 표시한다. 배포 여부는 별도로 기록한다.

## To do

- [x] 0. 현행 로직·개선안 저장, 운영 문서의 폐기된 시드·명령·네트워크 설명 제거.
- [x] 1a. 프런트엔드 한영 부분 수정에서 생략한 언어 보존. 명시적인 빈 값만 삭제로 처리.
- [x] 1b. 상세 절 수정·삭제 전후 원문과 출처 보관, 이력에서 복원하는 회귀 검증.
- [x] 1c. 별칭·경력·학습 장면의 개별 추가·수정·삭제 계약 도입. 기존 전체 교체와 구분.
- [x] 2a. 읽은 편집 버전을 제출하는 충돌 검사와 모든 쓰기 경로의 잠금 통일.
- [x] 2a-1. JS Admin/API/CLI의 선택적 expectedRevision 검사, 인물·절 동시 편집/삭제 충돌 검증.
- [x] 2a-2. Python 직접 반영·제안 승인과 나머지 쓰기 경로의 공통 잠금/버전 계약 적용. 버전 없는 기존 클라이언트 전환.
- [x] 2b. Python·Admin·CLI 저장 규칙 통합: 공통 스키마, 출처, 필수 역할, 중복, 분량 검증.
- [x] 3. 보강 완료·해당 없음·출처 부족 상태와 재검토 조건, 품질 중심 우선순위 도입.
- [x] 4. 주장별 출처·쪽수·근거 연결, 출처 충돌·동일인 불명·대량 삭제의 선별 검토.
- [x] 5. 통합 검증과 운영 반영, 최종 실행 계약 문서 갱신.

## 현재 구현

Python 인물/절 직접 SQL과 승인 경로를 제거하고 JS 공통 서비스로 통합했다.
현재 실행 계약은 [인물 운영 문서](commulingo_people_handoff.md)의
버전·출처·검토·보강 상태 절을 따른다. 아래 기록은 각 단계 당시의 검증/배포 이력이다.
현재 운영 설명으로 과거 선택적 버전·Python 이중 저장 설명을 사용하지 않는다.

## 개선 방향과 완료 기준

1. 데이터 보존: 언어별 병합과 명시적 삭제 계약을 정하고, 절 이력에 전후 원문·출처·정렬을 남긴다. 별칭·경력 개별 작업은 전체 목록 재전송을 줄인다. 잘못된 입력과 중간 실패는 내용·이력 모두 롤백해야 한다.
2. 저장 일관성: expectedRevision과 공통 잠금으로 오래된 편집을 감지한다. AI·관리자·CLI가 같은 쓰기 서비스를 사용하도록 통합하거나 우선 공통 스키마/교차 검증으로 격차를 줄인다. 현재 Python에만 있는 분량·중복 등 검증, JS의 빈 출처 허용과 role:null 허용을 정리한다. 데이터 제약과 문체 권고를 구분한다.
3. 품질 중심 보강: 절 수는 상한이지 목표가 아니다. 오류 → 출처 부족 → 한영 불일치 → 핵심 생애·사상 누락 → 관계·문체 보완 순서로 선정한다. 완료/해당 없음/출처 부족 상태와 새 근거 또는 재검토 시점을 기록해 반복 작업을 피한다. 관계 수가 다시 중요도를 높이는 순환도 완화한다.
4. 근거: 생몰년·국적·일화·평가 등 주장별로 출처와 쪽수/구절을 연결한다. 충돌하는 근거도 보관한다. 자동 반영은 유지하되 출처 충돌·동일인 불명·대규모 삭제만 검토로 분기한다. AI confidence 단독으로 승인하지 않는다.

## 검증·반영 기록

- 시작: 운영 데이터·스냅샷을 쓰지 않는 작업 사본에서 1a/1b 구현. 독립 PostgreSQL 테스트로 저장·이력·복원을 검증하고 일반 npm test를 실행한다.
- 기존 사용자 변경(README 색인과 DB 후속 후보 문서, 미추적 콘텐츠 자료)은 보존한다. DB 후속 후보 문서의 다른 과제는 이번 인물 편집 작업의 범위에 자동 포함하지 않는다.

### 2026-09-07: 0, 1a, 1b 완료

- 운영 문서에서 폐기된 people.js 시드, 재적재/검증 명령, ROLE_RULES fallback, 옛 IPv6 네트워크 요구를 제거했다. 캐시 설명을 현재 기본 60초로 맞췄다. 기존에 유효한 사건 그룹·국가 태그 설명은 유지했다.
- JS store의 한영 소개·수식어·일화·성/이름·legacy 이름·fate 라벨·국적 라벨을 생략 보존으로 변경했다. aliases도 언어별 생략을 보존한다. 부칭의 기존 병합 규칙은 유지했다. 국적 코드 변경 시 이름을 재조합하고 이전 국적 라벨은 새 국적에 옮기지 않는다.
- 상세 절 upsert는 생략한 언어·출처·정렬을 보존한다. 이력의 기존 before/after/sections 필드에 sectionBefore/sectionAfter를 추가했다. 변경 전 절을 같은 upsert 경로로 전달해 본문·출처·정렬을 복원한다. 타임스탬프는 복원 실행 시의 값이다.
- 검증: 운영 테이블 정의와 검증 함수만 추출한 PostgreSQL 17 일회용 DB(운영망·볼륨·포트와 분리, 운영 데이터 없음)에서 `scripts/test-commulingo-person-edit-db.js` 통과. 한국어만 수정, 복합 영어 성 보존, 국적별 이름 재조합, 명시적 비우기, 잘못된 입력 거부, 절 수정 취소/삭제 복원, 내용·이력 롤백 검증.
- 검증: `npm test` 전체 통과(새 언어 병합 smoke와 lint 포함).
- 반영: 검증한 코드 파일만 원자적 rename으로 작업 트리에 설치. 운영 DB 변경·컨테이너 재시작·배포는 아직 하지 않았다. data/ 코드가 호스트 마운트이므로 실행 중 프로세스와 새 프로세스의 require 캐시 상태를 구분해야 한다.
- 다음: 1c 개별 목록 작업 계약, 이어서 2a 편집 버전 검사. Python 쓰기 경로, whole-person 삭제 복원, 과거 이력의 원문 누락, 자동 보강 완료 기준은 이번 1a/1b 구현으로 해결되었다고 간주하지 않는다.

### 2026-09-07: 1c, 2a-1 구현

- PATCH/기존 upsert CLI에 aliasEdits, careerEdits, sceneEdits를 추가했다. op은 add/update/remove이고 각각 언어+별칭 문자열, 경력 DB id, collection/episode 쌍으로 대상을 지정한다. 해당 전체 교체 필드와 동시에 보내면 400이며, 잘못된 참조·중복·알 수 없는 키도 400으로 거부한다. 요청당 각 작업 배열은 최대 100개다.
- 경력 개별 수정은 행을 삭제 후 재생성하지 않는다. 다른 경력의 ID·정렬·내용은 유지하고, 수정한 경력의 생략한 언어·기간도 보존한다. 추가는 목록 끝에 붙고 삭제는 지정 행만 제거한다. 전체 계획을 먼저 검증한 뒤 같은 인물 잠금/트랜잭션/이력 경로에서 실행한다.
- Admin getPerson은 읽기 전용 REPEATABLE READ에서 카드와 자식 행을 읽고 revision을 반환한다. revision은 영속 인물·별칭·경력·장면·부칭·역할·상세 절·직책 행 상태로 계산한다. 따라서 부모 updated_at을 갱신하지 않은 외부 자식 행 수정도 커밋된 상태라면 감지한다.
- 인물 PATCH와 절 PUT 본문의 expectedRevision, 인물/절 DELETE 본문의 expectedRevision을 잠금 후 검사한다. 불일치는 409 revision_conflict/currentRevision, 잘못된 형식은 400이며 변경과 이력을 쓰지 않는다. 기존 클라이언트는 생략할 수 있으므로 모두가 자동 보호되는 것은 아니다. Python의 동시 쓰기 잠금/버전 통일은 아직 미완료다.
- 검증: 독립 PostgreSQL 17에서 기존 편집/복원/롤백 테스트 확장분 통과. 새 동시성 테스트는 두 연결의 실제 Lock 대기를 확인하고 인물 수정·절 수정·절 삭제·인물 삭제 모두 오래된 요청을 거부함을 확인했다. 외부 경력 수정이 버전을 무효화하는 것도 검증했다. 운영 인물 데이터·운영 DB는 변경하지 않았다.
- 코드 반영: 검증한 파일만 원자적으로 작업 트리에 설치했다. 프런트엔드 이미지 재시작/배포는 아직 하지 않았다.
- 최종 검증: `npm test` 전체와 lint 통과. `git diff --check` 통과. 일회용 테스트 컨테이너는 검증 후 종료한다.

### Python 경로 및 나머지 개선 구현·검증

- [x] Python 인물/절 저장·승인 → 고정 Docker stdin RPC → JS Admin service로 통합. SQL fallback 차단.
- [x] 기존 인물/절 버전 필수, 공통 advisory lock, 저장·근거·이력·승인 원자성.
- [x] 공통 분량/출처/역할/중복 검사, 주장별 근거 및 검토 대기·승인·거절.
- [x] 주제별 완료/해당 없음/출처 부족과 90/180일 재검토, 새 근거 재개방, 품질 중심 선정.
- [x] 독립 DB 통합·동시성·복원, 실제 Python RPC, 선택 쿼리, pending terminal 테스트.
- [x] 운영 176 적용·양쪽 코드 배포·조회·서비스 상태 최종 확인.

기존 2,201명에 대한 주장 근거 소급 작성은 이번 코드 배포의 자동 효과가 아니다.
보강기가 출처 없는 인물을 우선 대상으로 삼으며 근거의 진위 판정은 조사/검토 책임이다.

### 최종 운영 반영 완료

- 운영 DB: migration 176 적용, frontend 역할의 새 테이블/시퀀스 권한 확인.
  기존 인물 2,201명 유지. 인물/자식/직책/이력/제안 사전 백업은
  `/tmp/commulingo-before-176.dump`(약 51 MB), 파일 목록 검증 완료.
- Frontend: `bb113868ad8b170e595abeac303d61b68c5b84fe`,
  `deploy/commulingo-editorial-20260907`에서 공식 `scripts/deploy --restart` 배포.
  healthy, UID/GID 1000:1000, 기존 data 마운트와 leninbot_default 연결 유지.
- Python: `5e95beb` → 최종 `233cfae`. 공통 RPC/승인/보강 상태 구현 및 국적 일괄 도구의 직접 SQL 경로 제거.
  두 일괄 도구는 보고서가 기본이며 `--apply-spec`만 공통 Admin CLI로 저장한다.
  미사용 인물 자식 SQL 함수도 제거했다. API/Telegram 재시작, 보강 배치 타이머 복귀.
- 검증: 전체 npm test/lint, 20개 경로 HTTP 200, code/DB drift,
  2,201명 인물 감사 통과. 운영 Python→JS RPC와 READ ONLY 보강 선정 쿼리 통과.
  Python 일반 회귀 17개 통과(RPC는 일반 실행에서 1개 skip), 별도 실제 RPC/CLI 통합 및
  배치 실패 전체 롤백 통과, 국적 조사 smoke 2개 통과. 독립 DB에서 편집·복원·동시성·
  근거·검토 승인/오래된 승인 거부·상태 재개방을 검증했다.
- 복구 자료: 이전 이미지 `leninbot-frontend:before-editorial-176-20260907`,
  이전 JS 파일 `/tmp/commulingo-before-176-code`, 이전 Python `/tmp/commulingo-before-176-python`.
  영구 이력은 양쪽 Git 커밋으로 관리한다. 추가 테이블은 코드 롤백 시 삭제하지 않는다.
- 기존 콘텐츠 자료 및 다른 DB 후속 과제 문서는 보존했다. 이 체크리스트의 구현·배포 작업은 완료했다.

## 승인 운영 보완 To do

- [x] 독립 검토 에이전트와 근거 재확인·판단 검증.
- [x] 영속 검토 큐·재시도·충돌 처리·중복 실행 방지.
- [x] 판단 불가 건 소유자 DM과 조회/승인/반려/재검토 명령.
- [x] 독립 DB·에이전트·Telegram 권한 회귀 검증.
- [x] 운영 스키마·서비스·타이머 반영 및 실제 실행 검증.

### 승인 운영 보완 반영

- Python `444bd6c`: 독립 검토 에이전트, 영속 큐/lease/재시도, 근거 재확인과 승인/반려,
  판단 불가 소유자 DM, `/commulingo_review` 조회·승인·반려·재검토 명령 반영.
- 검토 에이전트는 작성자 대화를 이어받지 않고 원문을 새로 가져온다. 인용의 원문 포함,
  제안 출처 확인, 검토 사유 해소를 검사한 뒤 공통 JS 서비스로만 반영한다.
- 운영 migration 177 적용, 기존 인물 2,201명 유지. 제안 백업:
  `/tmp/commulingo-before-177-suggestions.dump`; 이전 Python 파일:
  `/tmp/commulingo-before-review-python`.
- `leninbot-commulingo-review.service` 첫 운영 실행 exit 0, status=idle, cost_usd=0.
  당시 대기 제안은 0건이어서 운영 승인/반려나 시험 메시지는 만들지 않았다.
  실제 승인·경합·재시도·알림 동작은 독립 DB/모의 외부 호출로 검증했다.
- `leninbot-commulingo-review.timer` 활성화: 15분마다 최대 한 건, 조사 제한 480초,
  한 건 $0.20/12라운드, 기존 일일 합산 $2.5 제한. 예산 소진 시 알림은 계속 확인한다.
- 실패 최대 3회/1시간 간격, 판단 불가 소유자 전달, 실패 전송 재시도,
  미처리 하루 뒤 재알림. Telegram 재시작 및 명령 메뉴 반영.
- 검증: 정책·권한·실행 문맥·스키마·상태 집계 8개, 독립 DB 통합 6개 통과.
  소유자 반려와 자동 승인 경합, 오래된 버전 반려, lease 만료/재획득, 3회 실패,
  알림 재시도/중복 억제·소유자 재검토 검증. systemd unit 문법과 일정 검증 통과.
- 프런트엔드 런타임은 기존 공통 승인 서비스를 사용하므로 이미지 재빌드는 필요하지 않았다.
  자동 검토 역시 역사적 진위의 무오류를 보증하지 않으며, 판단 근거는 감사 가능하게 보관한다.
